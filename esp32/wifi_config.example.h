// WiFi configuration - copy to wifi_config.h and fill in your credentials
// wifi_config.h is in .gitignore, so it won't be committed

#ifndef WIFI_CONFIG_H
#define WIFI_CONFIG_H

const char* WIFI_SSID = "CAPI";
const char* WIFI_PASSWORD = "NOAH2016";

#endif
